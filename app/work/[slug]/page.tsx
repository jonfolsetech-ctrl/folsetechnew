import{notFound}from"next/navigation";import{caseStudies}from"@/lib/content";import{buildMetadata}from"@/lib/seo";
export function generateStaticParams(){return caseStudies.map(c=>({slug:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=caseStudies.find(x=>x.slug===slug);if(!c)return{};return buildMetadata({title:c.title,description:c.summary,path:`/work/${c.slug}`})}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=caseStudies.find(x=>x.slug===slug);if(!c)notFound();return <section className="section"><div className="container content"><span className="badge">DEMONSTRATION — NOT A CLIENT RESULT</span><h1>{c.title}</h1><p className="lead">{c.summary}</p>{["Client situation","Problem","Strategy","UX decisions","SEO strategy","Development approach","Performance improvements","Conversion strategy","Results","Technologies used"].map(h=><section key={h}><h2>{h}</h2><p>[Replace with verified client information, screenshots, measurements, and approved claims before publishing as a real case study.]</p></section>)}</div></section>}
