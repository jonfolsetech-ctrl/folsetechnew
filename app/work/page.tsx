import Link from"next/link";import{caseStudies}from"@/lib/content";import{buildMetadata}from"@/lib/seo";
export const metadata=buildMetadata({title:"Web Design Case Studies",description:"FolseTech case study framework for documenting strategy, UX, SEO, development, performance, conversion, and verified results.",path:"/work"});
export default function Page(){return <section className="section"><div className="container"><p className="eyebrow">WORK</p><h1>Case studies built around decisions and verified outcomes.</h1><p className="lead">Until real client work is approved for publication, this area uses clearly labeled demonstration content rather than invented claims.</p><div className="grid-3">{caseStudies.map(c=><article className="card" key={c.slug}><span className="badge">{c.placeholder?"DEMO / PLACEHOLDER":c.industry}</span><h2>{c.title}</h2><p>{c.summary}</p><Link className="link" href={`/work/${c.slug}`}>View framework →</Link></article>)}</div></div></section>}
