import{notFound}from"next/navigation";import Link from"next/link";import{articles}from"@/lib/content";import{buildMetadata,breadcrumbJsonLd}from"@/lib/seo";import{JsonLd}from"@/components/JsonLd";import{Breadcrumbs}from"@/components/Breadcrumbs";import{siteConfig}from"@/lib/site";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)return{};return buildMetadata({title:a.title,description:a.excerpt,path:`/insights/${a.slug}`})}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();const article={"@context":"https://schema.org","@type":"Article",headline:a.title,datePublished:a.date,dateModified:a.date,author:{"@type":"Organization",name:"FolseTech"},publisher:{"@type":"Organization",name:"FolseTech"},mainEntityOfPage:new URL(`/insights/${a.slug}`,siteConfig.url).toString(),description:a.excerpt};return <><div className="container"><Breadcrumbs items={[{name:"Home",href:"/"},{name:"Insights",href:"/insights"},{name:a.title}]}/><JsonLd data={breadcrumbJsonLd([{name:"Home",path:"/"},{name:"Insights",path:"/insights"},{name:a.title,path:`/insights/${a.slug}`}])}/><JsonLd data={article}/></div><article className="section"><div className="container content"><span className="badge">{a.category}</span><h1>{a.title}</h1><div className="article-meta"><span>Published {a.date}</span><span>•</span><span>6 min read</span><span>•</span><span>FolseTech editorial team</span></div><p className="lead">{a.excerpt}</p><h2>Start with page purpose</h2><p>Every indexable page should serve a distinct user need and a distinct role in the site architecture. That makes content easier to maintain, internal links more intentional, and search signals less ambiguous.</p><h2>Connect technical quality to user value</h2><p>Performance, crawlability, semantic structure, metadata, accessibility, and structured data matter most when they help people discover, understand, and use the page. Technical SEO should reinforce the experience rather than exist as a separate layer.</p><h2>Measure the journey</h2><p>Track actions tied to real business outcomes, then use those signals to improve content, calls to action, and navigation. Avoid making optimization decisions from pageviews alone.</p><p><Link className="button" href="/free-website-audit">Get a Free Website Growth Plan</Link></p></div></article></>}
