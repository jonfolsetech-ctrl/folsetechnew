import{notFound}from"next/navigation";import{industries}from"@/lib/content";import{buildMetadata,breadcrumbJsonLd}from"@/lib/seo";import{Breadcrumbs}from"@/components/Breadcrumbs";import{JsonLd}from"@/components/JsonLd";import{CTA}from"@/components/CTA";
export function generateStaticParams(){return industries.map(i=>({slug:i.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const i=industries.find(x=>x.slug===slug);if(!i)return{};return buildMetadata({title:`Web Design for ${i.title}`,description:i.description,path:`/industries/${i.slug}`})}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const i=industries.find(x=>x.slug===slug);if(!i)notFound();return <><div className="container"><Breadcrumbs items={[{name:"Home",href:"/"},{name:"Industries",href:"/industries"},{name:i.title}]}/><JsonLd data={breadcrumbJsonLd([{name:"Home",path:"/"},{name:"Industries",path:"/industries"},{name:i.title,path:`/industries/${i.slug}`}])}/></div><section className="page-hero"><div className="container content"><p className="eyebrow">{i.title.toUpperCase()}</p><h1>Web Design for {i.title}</h1><p className="lead">{i.description}</p></div></section><section className="section"><div className="container content"><h2>Customer journey</h2><p>Pages should answer the questions buyers have before they are ready to contact a provider, then make the next logical action easy. Decision-stage content, proof, service detail, and clear navigation should work together.</p><h2>Search opportunity</h2><p>Build topic coverage around real services, problems, comparisons, and educational needs rather than creating near-duplicate keyword pages. Every indexable page should have a distinct job.</p><h2>Conversion strategy</h2><p>Use offers and calls to action that match visitor intent, from low-friction education to a direct consultation. Track meaningful events so optimization is based on behavior rather than guesswork.</p><h2>Technical priorities</h2><ul><li>Fast mobile experience and stable layouts</li><li>Crawlable service and resource architecture</li><li>Accessible forms and clear trust signals</li><li>Structured data only where visible content supports it</li></ul></div></section><CTA /></>}
