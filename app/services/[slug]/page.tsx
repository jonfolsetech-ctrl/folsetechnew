import { notFound } from "next/navigation"; import Link from "next/link"; import { services } from "@/lib/content"; import { buildMetadata,breadcrumbJsonLd } from "@/lib/seo"; import { Breadcrumbs } from "@/components/Breadcrumbs"; import { JsonLd } from "@/components/JsonLd"; import { CTA } from "@/components/CTA";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)return {};return buildMetadata({title:`${s.title} Services`,description:s.short,path:`/services/${s.slug}`})}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <><div className="container"><Breadcrumbs items={[{name:"Home",href:"/"},{name:"Services",href:"/services"},{name:s.title}]}/><JsonLd data={breadcrumbJsonLd([{name:"Home",path:"/"},{name:"Services",path:"/services"},{name:s.title,path:`/services/${s.slug}`}])}/></div><section className="page-hero"><div className="container content"><p className="eyebrow">{s.title.toUpperCase()}</p><h1>{s.title} Services</h1><p className="lead">{s.description}</p><div className="actions"><Link className="button" href="/free-website-audit">Get a Free Website Growth Plan</Link><Link className="button button-ghost" href="/contact">Discuss Your Project</Link></div></div></section><section className="section"><div className="container content"><h2>Problems this service addresses</h2><ul>{s.pains.map(x=><li key={x}>{x}</li>)}</ul><h2>Business outcomes</h2><ul>{s.benefits.map(x=><li key={x}>{x}</li>)}</ul><h2>Technical capabilities</h2><ul>{s.capabilities.map(x=><li key={x}>{x}</li>)}</ul><h2>How the work is approached</h2><p>We start with the business model, audience, existing search equity, conversion path, and technical constraints. The resulting plan defines page purpose, information architecture, measurement, implementation priorities, and launch safeguards before development accelerates.</p><h2>Frequently asked questions</h2><h3>Is SEO included?</h3><p>Every FolseTech website is built with technical search fundamentals in mind. The depth of keyword research, content development, migration work, and ongoing SEO depends on project scope.</p><h3>Can this be combined with other services?</h3><p>Yes. Service architecture is intentionally modular, so {s.title.toLowerCase()} can be combined with technical SEO, Next.js development, conversion optimization, redesign work, or AI lead generation.</p></div></section><CTA /></>}
