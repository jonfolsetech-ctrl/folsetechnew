import Link from "next/link"; import { services } from "@/lib/content"; import { buildMetadata } from "@/lib/seo"; import { CTA } from "@/components/CTA";
export const metadata=buildMetadata({title:"Web Design, SEO & AI Services",description:"Explore FolseTech services for SEO-first web design, Next.js development, technical SEO, conversion optimization, AI lead generation, and redesigns.",path:"/services"});
export default function Page(){return <><section className="page-hero"><div className="container"><p className="eyebrow">SERVICES</p><h1>Web systems engineered for visibility, speed, and conversion.</h1><p className="lead">Choose a focused engagement or combine disciplines into a complete website growth system.</p></div></section><section className="section"><div className="container grid-3">{services.map(s=><article className="card" key={s.slug}><h2>{s.title}</h2><p>{s.description}</p><Link className="link" href={`/services/${s.slug}`}>Explore service →</Link></article>)}</div></section><CTA /></>}
